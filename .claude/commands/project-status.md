Read the file `tasks.json` in the project root and print a status report for the taskforge project.

Your report must include:

1. **Summary** — total number of tasks, how many are done, how many are pending
2. **By Status** — count of `pending` vs `done` tasks
3. **By Priority** — count of `low`, `medium`, and `high` priority tasks (treat missing priority field as `medium`)

Format the output clearly with headers and counts. Do not write any code or modify any files — just read tasks.json and report the numbers.
